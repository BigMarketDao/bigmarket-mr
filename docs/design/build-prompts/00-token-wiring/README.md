# 00 — Token Wiring

**Run first. Nothing else can start until this lands.**

| File | Model | What it does |
|------|-------|--------------|
| `A-token-wiring.md` | Haiku 4.5 | Adds all Wave 1+2 tokens to `theme.css` + `tokens.ts`. 3 files only. |

**Definition of done:** `bg-card`, `bg-popover`, `text-muted-foreground`, `price-up`, `outcome-1..4` (per `tokens-and-rules.lock.md`), `community`, etc. all compile to working Tailwind utilities in light + dark.
