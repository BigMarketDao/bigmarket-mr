/**
 * Shared horizontal rail for HeaderMenuDaisy and (later) main page content
 * so columns align with the top navigation.
 */
export const PAGE_CONTENT_RAIL =
	'mx-auto w-full max-w-7xl px-4 sm:px-6 lg:px-8' as const;
